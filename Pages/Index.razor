@page "/"
@inject NavigationManager NavigationManager;

@code {
    
    protected override Task OnInitializedAsync()
    {
        Task.Run(async() => NavigationManager.NavigateTo("allmylinks")).ConfigureAwait(false);
        return base.OnInitializedAsync();
    }
}